import { Provider } from "@aws-sdk/types";
export declare const ENV_REGION = "AWS_REGION";
export interface EnvConfiguration {
    environmentVariableName?: string;
}
export declare function fromEnv({ environmentVariableName }?: EnvConfiguration): Provider<string>;
