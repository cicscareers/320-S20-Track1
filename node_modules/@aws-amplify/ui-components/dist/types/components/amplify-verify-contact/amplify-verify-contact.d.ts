import { AuthStateHandler, CognitoUserInterface } from '../../common/types/auth-types';
export declare class AmplifyVerifyContact {
    /** Authentication state handler */
    handleAuthStateChange: AuthStateHandler;
    /** User with unverified contact information  */
    user: CognitoUserInterface;
    verifyAttr: any;
    loading: boolean;
    handleSubmit(event: any): void;
    submit(code: any): Promise<void>;
    verify(contact: keyof CognitoUserInterface['unverified']): Promise<void>;
    renderSubmit(): any;
    renderVerify(): any;
    render(): any;
}
