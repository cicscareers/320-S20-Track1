import { AuthState, CognitoUserInterface, FederatedConfig, UsernameAliasStrings } from '../../common/types/auth-types';
export declare class AmplifyAuthenticator {
    /** Initial starting state of the Authenticator component. E.g. If `signup` is passed the default component is set to AmplifySignUp */
    initialAuthState: AuthState.SignIn | AuthState.SignUp;
    /** Federated credentials & configuration. */
    federated: FederatedConfig;
    /** Username Alias is used to setup authentication with `username`, `email` or `phone_number`  */
    usernameAlias: UsernameAliasStrings;
    authState: AuthState;
    authData: CognitoUserInterface;
    toastMessage: string;
    componentWillLoad(): Promise<void>;
    checkUser(): Promise<void>;
    onAuthStateChange: (nextAuthState: AuthState, data?: CognitoUserInterface) => any;
    renderAuthComponent(authState: AuthState): any;
    componentDidUnload(): Promise<void>;
    render(): any;
}
