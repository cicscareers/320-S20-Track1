import { CognitoUserInterface, AuthStateHandler } from '../../common/types/auth-types';
import { TOTPSetupEventType } from './amplify-totp-setup-interface';
export declare class AmplifyTOTPSetup {
    inputProps: object;
    /** Used in order to configure TOTP for a user */
    user: CognitoUserInterface;
    /** Auth state change handler for this component */
    handleAuthStateChange: AuthStateHandler;
    code: string | null;
    setupMessage: string | null;
    qrCodeImageSource: string;
    qrCodeInput: string | null;
    loading: boolean;
    componentWillLoad(): void;
    buildOtpAuthPath(user: CognitoUserInterface, issuer: string, secretKey: string): string;
    checkContact(user: CognitoUserInterface): Promise<void>;
    onTOTPEvent(event: TOTPSetupEventType, data: any, user: CognitoUserInterface): void;
    handleTotpInputChange(event: any): void;
    generateQRCode(codeFromTotp: string): Promise<void>;
    setup(): Promise<void>;
    verifyTotpToken(event: Event): Promise<void>;
    render(): any;
}
